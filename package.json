{
  "name": "keyverse",
  "version": "0.1.0",
  "private": true,
  "description": "keyverse: multipack scripture note door. Primary host is Elixir (mix); server.mjs is legacy reference only.",
  "type": "module",
  "scripts": {
    "start": "mix run --no-halt",
    "dev": "mix run --no-halt",
    "test": "mix test",
    "check": "mix compile --warnings-as-errors",
    "legacy:node": "node server.mjs",
    "extract:client": "node scripts/extract_client_js.mjs"
  },
  "dependencies": {
    "grab-bcv": "^0.1.7",
    "qrcode": "^1.5.4"
  }
}
